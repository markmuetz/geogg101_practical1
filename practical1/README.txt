All results are generated on WinXP VM then copied to results/ from
/tmp/scratch/ucfamue. each output file has a name like 
model_n<manning_n>_ev<eddy_viscosity>.txt. The format is not quite CSV, there
is no comma in between cols 2 and 3.

data_files/ contains supplementary data files for the practical (downloaded
from moodle). Includes observed data.
